namespace Game.ECS.Components
{
    public struct PrototypeComponent
    {
        public string PrototypeId;

        public PrototypeComponent(string prototypeId)
        {
            PrototypeId = prototypeId;
        }
    }
}
